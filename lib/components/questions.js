// import { el } from '../elements.js';

/**
 *
 * @param root root element
 * @param keywords keywords arrayið úr json skjalinu
 */
export function renderQuestions(root, questions) {
  console.log("rendering keywords page", root, questions);
}
